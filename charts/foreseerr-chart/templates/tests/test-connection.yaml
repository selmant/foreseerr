apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foreseerr.fullname" . }}-test-connection"
  labels:
    {{- include "foreseerr.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "foreseerr.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
