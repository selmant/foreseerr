apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foreseer.fullname" . }}-test-connection"
  labels:
    {{- include "foreseer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "foreseer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
