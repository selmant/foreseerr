#!/usr/bin/env sh

# Skip Commitizen in non-interactive environments (CI/agents).
if [ -n "$CI" ] || [ ! -t 1 ]; then
  exit 0
fi

exec < /dev/tty && bun --bun ./node_modules/commitizen/bin/git-cz --hook || true
