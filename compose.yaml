services:
  foreseerr:
    build:
      context: .
      dockerfile: Dockerfile.local
    ports:
      - 3000:3000
      - 5055:5055
    volumes:
      - .:/app:rw,cached
      - /app/node_modules
      - /app/dist/public
