---
# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: Test Docs

on:
  pull_request:
    branches:
      - develop
    paths:
      - 'docs/**'
      - 'gen-docs/**'

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  test-build:
    name: Test build
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          fetch-depth: 0
          persist-credentials: false

      - parallel:
          - name: Set up Node.js
            uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
            with:
              node-version-file: .node-version
              package-manager-cache: false

          - name: Pnpm Setup
            uses: pnpm/action-setup@0e279bb959325dab635dd2c09392533439d90093 # v6.0.8

      - name: Get pnpm store directory
        shell: sh
        run: |
          echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - name: Setup pnpm cache
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-docs-${{ hashFiles('gen-docs/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-docs-

      - name: Install dependencies
        working-directory: gen-docs
        # gen-docs is outside the root workspace; ignore parent so docusaurus lands in gen-docs/node_modules
        run: pnpm install --frozen-lockfile --ignore-workspace

      - name: Build website
        working-directory: gen-docs
        run: pnpm gen-api-docs && pnpm build
